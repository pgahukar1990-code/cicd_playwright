name: Playwright Parameterized MultiBrowser Parallel with Allure Reporting

on:
  workflow_dispatch:
    inputs:
      test_target:
        description: "Test file or pattern (e.g. dropdown.spec.js, tests/auth/, -g 'title')"
        required: false
        default: ""

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        project: [chromium, firefox, Google Chrome]   # 👈 multiple browsers
        shard: ["1/3", "2/3", "3/3"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests with Allure
        run: |
          TARGET=""
          if [ -n "${{ github.event.inputs.test_target }}" ]; then
            if [[ "${{ github.event.inputs.test_target }}" == *.spec.* ]]; then
              TARGET="tests/${{ github.event.inputs.test_target }}"
            else
              TARGET="${{ github.event.inputs.test_target }}"
            fi
          fi

          # run tests
          npx playwright test $TARGET \
            --project="${{ matrix.project }}" \
            --shard=${{ matrix.shard }} \
            --reporter=line,allure-playwright

      - name: Upload Allure results
        if: always()
        run: |
          SAFE_SHARD=$(echo "${{ matrix.shard }}" | sed 's/\//-/g')
          mkdir -p renamed-results/$SAFE_SHARD
          cp -r allure-results/* renamed-results/$SAFE_SHARD/ || true

        # upload renamed artifacts
      - name: Upload renamed Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-${{ matrix.project }}-${{ matrix.shard }}
          path: renamed-results

  merge-report:
    runs-on: ubuntu-latest
    needs: test
    if: always()
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./allure-results

      - name: Merge and generate Allure report
        run: |
          mkdir -p merged-results
          find ./allure-results -type d -exec cp -r {}/* merged-results/ \;
          npx allure generate merged-results --clean -o allure-report

      - name: Upload merged Allure report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report
          retention-days: 30
